#include<stdio.h>
int main()
{
	int arr[5], i,max=0,min;
	
	for(i=0;i<=4;i++)
	{
		printf("Enter Number  ");
		scanf("%d",&arr[i]);
		
	}
	
	for(i=0;i<=4;i++)
	{
	
	  if(arr[i]>=max)
	  {
	  		max=arr[i];
	   	
	  }
		
	}
for(i=0;i<=4;i++)
{
	min=
	  if(arr[i]<=)
	   	{
	   		min=arr[i];
	    }
	   	
	  }
		
	printf("Maximum = %d",max);
	printf("Minimum = %d",min);
	
	return 0;
}
